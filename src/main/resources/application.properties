# ====================== 服务端口 ======================
server.port=8765

# ====================== 文件上传 ======================
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ====================== 元数据库 (存连接配置+任务配置) ======================
# 默认: H2 文件持久化
spring.datasource.url=jdbc:h2:file:./data/autodb;AUTO_SERVER=TRUE;MODE=MySQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# ---- 切换元数据库为 MySQL ----
# spring.datasource.url=jdbc:mysql://localhost:3306/apirk_meta?useSSL=false&serverTimezone=Asia/Shanghai
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=your_password

# ---- 切换元数据库为 PostgreSQL ----
# spring.datasource.url=jdbc:postgresql://localhost:5432/apirk_meta
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=postgres
# spring.datasource.password=your_password

# ====================== H2 Web Console (开发调试) ======================
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ====================== Thymeleaf ======================
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
